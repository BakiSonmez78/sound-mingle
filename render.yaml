# Triggering Force Deploy via Infrastructure Change
services:
  - type: web
    name: sound-mingle
    env: node
    plan: free
    buildCommand: npm install && npm run build
    startCommand: npm start
    envVars:
      - key: NODE_VERSION
        value: 20.10.0
